// eslint-disable-next-line no-unused-vars
const storeName = 'Apple';

// eslint-disable-next-line no-unused-vars
const storeDescription = {
    budget: 10000,
    employees: ['Oleg', 'Nikita', 'Denis'],
    products: { iphone: 1000, ipad: 1200 },
    open: true
};